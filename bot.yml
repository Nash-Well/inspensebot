settings:
  token_env: TOKEN
  parse_mode: html
  long_poller: {}

commands:
  /start: 'Запуск бота'

buttons:
  add: '{{ text `btn_add` }}'
  settings: '{{ text `btn_settings` }}'

  fin_type: &type
    unique: type
  inc:
    <<: *type
    text: '{{ text `btn_inc` }}'
    data: income
  exp:
    <<: *type
    text: '{{ text `btn_exp` }}'
    data: expense

  lang: &lang
    unique: lang
  uk:
    <<: *lang
    data: 'uk'
    text: '🇺🇦'
  en:
    <<: *lang
    data: 'en'
    text: '🇬🇧'

markups:
  menu:
    - [ add ]
    - [ settings ]
  lang_menu:
    - [ uk, en ]
  type_menu:
    - [ inc, exp ]